---
paths:
  - "raw/**"
---

# raw/ 取り扱いルール

`raw/` は**ソースの置き場**。入れたら変更しない。Claude は**読むだけ**。

- 編集・整形・リネーム・削除をしない
- 内容を使いたいときは wiki 側に新しいページを作る（`/wiki-ingest`）
- カテゴリ分けは投入者が決める。Claude が勝手に move しない

理由: raw は「あの時そう書いてあった」という原本の役割を持つ。手を入れると原本でなくなる。
